import { HyphenatorPipe } from './hyphenator.pipe';

describe('HyphenatorPipe', () => {
  it('create an instance', () => {
    const pipe = new HyphenatorPipe();
    expect(pipe).toBeTruthy();
  });
});
