import { TitleCaseInputDirective } from './title-case-input.directive';

describe('TitleCaseInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleCaseInputDirective();
    expect(directive).toBeTruthy();
  });
});
