import { AutofocuserDirective } from './autofocuser.directive';

describe('AutofocuserDirective', () => {
  it('should create an instance', () => {
    const directive = new AutofocuserDirective();
    expect(directive).toBeTruthy();
  });
});
